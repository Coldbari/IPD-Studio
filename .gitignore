node_modules
dist/
coverage/
*.local
.DS_Store
.gstack/
test-results/
playwright-report/
.firebase/

# Claude Code worktrees/session state
.claude/
tsconfig.tsbuildinfo

# Stray scratch files
zz*.html

# Local secrets & per-developer backend config (.env.example is committed)
.env
.env.*
!.env.example

# Points the firebase CLI at ONE project — a fork must supply its own
# (see .firebaserc.example)
.firebaserc

# Internal working documents — specs, plans and spike notes we keep for
# ourselves. Not part of what the project publishes.
docs/superpowers/
docs/ENGINEERING-PLATFORM-PLAN.md
docs/DWG-IMPORT-SPIKE.md
